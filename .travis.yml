language: php

sudo: false

php:
  - 7.3
  - 7.4
  - 8.0
  - hhvm

before_script:
  - composer install

script:
  - ./vendor/bin/php-cs-fixer fix --verbose --diff --dry-run
  - composer test

matrix:
  allow_failures:
    - php:
      - hhvm
      - 8.0
